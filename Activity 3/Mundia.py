# Write a program to prompt the user for hours and rate per hour to compute gross pay.

hour = float(input("Enter number of hours:")) # will ask the user to enter number of hours work.
rate = float(input("Enter rate:")) # will ask the user to enter the rate per hour.
grosspay= hour * rate 
print (grosspay)
